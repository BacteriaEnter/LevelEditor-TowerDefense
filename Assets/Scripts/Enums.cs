using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enums 
{
   
}

public enum PlaceableType
{
    None,
    Tower,
    Trap
}

public enum PromptType_Block
{
    Range,
    Position
}

public enum TowerFacing
{
    Left,
    Top,
    Right,
    Bottom,
}

public enum DamageType
{
    Physics,
    Magic,
    True
}

public enum spOrientation
{
    BottomLeft,
    BottomRight,
    TopLeft,
    TopRight,
}
